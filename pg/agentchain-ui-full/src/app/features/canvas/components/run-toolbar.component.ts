import { Component,Input,Output,EventEmitter } from '@angular/core';
@Component({selector:'app-run-toolbar',template:`<mat-toolbar class="runbar"><button mat-raised-button color="primary" (click)="save.emit()"><mat-icon>save</mat-icon>Save</button><button mat-raised-button color="accent" (click)="run.emit()"><mat-icon>play_arrow</mat-icon>Run</button><button mat-stroked-button color="warn" *ngIf="canCancel" (click)="cancel.emit()"><mat-icon>stop</mat-icon>Cancel</button><mat-chip>{{status}}</mat-chip><span class="spacer"></span><span class="muted" *ngIf="dirty">Unsaved changes</span></mat-toolbar>`,styles:[`.runbar{background:#fff;border-radius:12px;margin-bottom:12px;gap:8px}`]})
export class RunToolbarComponent{@Input()status='idle';@Input()dirty=false;@Output()save=new EventEmitter<void>();@Output()run=new EventEmitter<void>();@Output()cancel=new EventEmitter<void>();get canCancel(){return ['pending','running','awaiting_human'].includes(this.status);}}
