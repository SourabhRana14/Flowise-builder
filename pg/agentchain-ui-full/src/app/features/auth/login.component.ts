import { Component } from '@angular/core';import { AuthService } from '../../core/services/auth.service';
@Component({selector:'app-login',template:`<div class="login-wrap"><mat-card><mat-card-title>AgentChain Login</mat-card-title><mat-card-content><mat-form-field class="full-width"><mat-label>Email</mat-label><input matInput [(ngModel)]="email"></mat-form-field><mat-form-field class="full-width"><mat-label>Password</mat-label><input matInput type="password" [(ngModel)]="password"></mat-form-field><button mat-raised-button color="primary" class="full-width" (click)="login()">Login</button><p class="error" *ngIf="error">{{error}}</p><p class="muted">Default: admin&#64;agentchain.local / admin123</p></mat-card-content></mat-card></div>`,styles:[`.login-wrap{height:100vh;display:grid;place-items:center;background:linear-gradient(135deg,#eef2ff,#fdf2f8)}mat-card{width:420px;padding:18px}`]})
export class LoginComponent{email='admin@agentchain.local';password='admin123';error='';constructor(private auth:AuthService){}login(){this.auth.login(this.email,this.password).subscribe({error:e=>this.error=e?.error?.message||'Login failed'});}}
